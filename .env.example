# Discord Bot Token (REQUIRED)
DISCORD_TOKEN=your_bot_token_here

# LLM Configuration (REQUIRED)
# Supported providers: groq, openai, lmstudio, ollama
LLM_PROVIDER=groq
MODEL=llama3-70b-8192

# Provider-specific API Keys
# For Groq (recommended)
GROQ_API_KEY=your_groq_api_key_here

# For OpenAI
# OPENAI_API_KEY=your_openai_key_here

# For local models (lmstudio/ollama) - requires LLM_URL
# LLM_URL=http://localhost:1234/v1  # LM Studio
# LLM_URL=http://localhost:11434/v1 # Ollama

# Database Configuration (REQUIRED)
# Used for Balatro game persistence, currency system, and per-server config
DB_HOST=localhost
DB_PORT=3306
DB_USER=admin
DB_PASSWORD=your_mysql_password_here
DB_NAME=cowsay

# Optional Configuration
# Separate model for intent detection (defaults to main MODEL)
# INTENT_MODEL=llama3-8b-8192

# Enable debug logging
# DEBUG_LOGGING=true